apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: localega
  labels:
    app: minio
spec:
  clusterIP: None
  ports:
    - port: 9000
      name: minio
  selector:
    app: minio
---
apiVersion: v1
kind: Service
metadata:
  name: minio-service
  namespace: localega
  labels:
    app: minio
spec:
  type: NodePort
  ports:
    - port: 9000
  selector:
    app: minio
