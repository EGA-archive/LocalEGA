apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: localega-inbox-storage
spec:
  storageClassName: inbox
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: localega-db-storage
spec:
  storageClassName: postgres
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi
